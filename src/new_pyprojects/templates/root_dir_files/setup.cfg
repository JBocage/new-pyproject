[metadata]
name = financial_document_extraction
version = 0.0.1
description = Financial Document Extraction

[options]
package_dir=
    =source
packages=find:

[options.packages.find]
where=source

[options.entry_points]
console_scripts =
    fde-cli = fde_cli.main:cli
